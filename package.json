{
  "name": "tuvafoto",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run db:up && npm run dev:all",
    "dev:all": "concurrently -n API,WEB -c yellow,cyan -k \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "dev": "npm run dev --workspace apps/web",
    "db:up": "docker compose up -d --wait",
    "db:stop": "docker compose stop",
    "db:reset": "npm run db:up && npm run db:reset --workspace apps/api",
    "generate": "npm run prisma:generate --workspace apps/api",
    "migrate": "npm run prisma:migrate:deploy --workspace apps/api",
    "seed": "npm run seed --workspace apps/api",
    "setup": "npm install && npm run generate && npm run db:up && npm run migrate && npm run seed",
    "build": "npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
